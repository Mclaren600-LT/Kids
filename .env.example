# Copy to .env.local for local dev and set the same value as FAMILY_PIN in
# the Pages dashboard for production. Frontend sends this as the X-Pin header.
# Leave both unset for an open endpoint (use Cloudflare Access instead).
VITE_FAMILY_PIN=
